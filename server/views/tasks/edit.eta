<% layout("../layouts/application", { header: it.t("views.tasks.edit.title") }) %>
<%~ include('./form', { ...it, heading: it.t('views.tasks.edit.title'), submitText: it.t('views.tasks.edit.submit') }) %>
