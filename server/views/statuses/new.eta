<% layout("../layouts/application", { header: it.t("views.statuses.new.title") }) %>
<h1 class="page-title"><%= it.t('views.statuses.new.title') %></h1>
<% if (it.errors) { const errs = Array.isArray(it.errors) ? it.errors : Object.values(it.errors).flat().map((e) => (typeof e === 'object' && e !== null) ? e : { message: e }); %>
  <div role="alert" class="mb-4 rounded border border-red-300 bg-red-50 px-3 py-2 text-red-700">
    <ul class="list-disc space-y-1 pl-4"><% errs.forEach((e) => { %><li class="text-sm"><%= e.message ?? e %></li><% }) %></ul>
  </div>
<% } %>
<section class="card mx-auto max-w-2xl">
  <form class="space-y-3" action="/statuses" method="post">
    <label class="block">
      <span class="label"><%= it.t('views.statuses.name') %></span>
      <input class="field" type="text" name="data[name]" id="data_name" value="<%= it.status?.name ?? '' %>">
    </label>
    <button type="submit" class="btn btn-primary"><%= it.t('views.statuses.new.submit') %></button>
  </form>
</section>
